1) Creating a local repository
2) Creating main.cpp with function getSum which takes a vector and return the sum of its elements 
3) push the repository online to my github account
4) creating a new branch called "First-Branch"
5) add to first branch function getAverg which takes a vector and return the average of its elements
6) push it to my repo
7) creating a new branch called "Second-Branch"
8) add to Second Branch function getMin which takes a vector and return the minimum element of it
9) push it to my repo
10) Merge first branch to main branch
11) Mergo Second branch to main branch but i got a conflict
12) resolved the conflict in VSC by keeping the 2 codes of the 2 branches but the main branch first.
